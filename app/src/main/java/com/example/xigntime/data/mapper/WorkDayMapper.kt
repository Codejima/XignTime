package com.example.xigntime.data.mapper

import com.example.xigntime.data.entities.WorkDay
import java.time.LocalDate
import java.time.LocalDateTime
import java.time.format.DateTimeFormatter
import java.util.*


/*
class WorkDayMapper() {
    val workDayDate:
}

fun WorkDay.toWorkDay(): WorkDay {
    val pattern = "yyyy-MM-dd"
    val formatter = DateTimeFormatter.ofPattern(pattern, Locale.getDefault())
    val localDateTime = LocalDateTime.parse(workDayDate, formatter)
    return WorkDay(
        workDayId = workDayId,
        workDayDate = localDateTime,
        profileId = profileId
    )
}*/
